import 'package:flutter/material.dart';

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Text("Faseeh Ahmed Id: m.faseeh1990@gmail.com"),
      ),
    );
  }
}

void main() {
  runApp(MyApp());
  runApp(MyApp());
}
